{
  "name": "brAIn-llm",
  "private": true,
  "license": "MIT",
  "description": "brAIn LLM-domain nodes — generic LLM caller, CLI agent wrapper.",
  "scripts": {
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0"
  },
  "version": "1.0.0"
}
